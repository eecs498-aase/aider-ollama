# The model runs on this machine. Copy to .env, or run: bin/use local
#
# 11434 is where Ollama puts itself, so this needs nothing from you beyond
# having Ollama running.
export OLLAMA_API_BASE=http://127.0.0.1:11434
