# The model runs on this machine.
#
#   cp .env.local .env
#
# 11434 is where Ollama puts itself, so this needs nothing from you beyond
# having Ollama running. aider reads .env from the directory you run it in.
export OLLAMA_API_BASE=http://127.0.0.1:11434
