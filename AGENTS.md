# House style

Loaded read-only into every aider session by `.aider.conf.yml`. Replace this
with the conventions of whatever you are building - it is here so the file
exists and the `read:` line resolves.

- Match the surrounding code. Its naming, its comment density, its idioms.
- Change what was asked for and nothing adjacent.
- Prefer editing an existing file over adding a new one.
