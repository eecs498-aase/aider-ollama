# The model runs on a CAEN lab machine, reached over the tunnel.
#
#   cp .env.caen .env
#
# 11435, not 11434, so this can be up at the same time as an Ollama running on
# your own machine. bin/ollama-tunnel start is what puts a model there.
export OLLAMA_API_BASE=http://127.0.0.1:11435
